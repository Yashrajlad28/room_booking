<h1>Edit Room</h1>

<%= form_with model: [:api, :v2, @room] do |f|%>
    <div>
        <%= f.label :name %>
        <%= f.text_field :name, value: @room.name %>
    </div>
    <div>
        <%= f.submit %>
    </div>
<% end %>

<%= link_to "Cancel", root_path %>